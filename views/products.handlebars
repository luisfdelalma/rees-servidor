<h2>Lista de productos</h2>
<p>A continuación se muestra una lista de 1000 productos paginados</p>

<div
    style="width:1000px; height: 700px; border:1px solid #000000; display:grid; grid-template-columns: repeat(2, 1fr); grid-template-rows: repeat(5, 1fr); grid-column-gap: 5px;grid-row-gap: 2px;">

    {{#each products}}
    <div style=" border: 2px solid #000000; padding:5px;">
        <b>Nombre:</b> {{this.title}}<br>
        <b>Código interno:</b> {{this.BId}}<br>
        <b>Precio:</b> ${{this.price}}<br>
        <b>Categoría:</b> {{this.category}}<br>
        <b>Descripción:</b> {{this.description}}
    </div>
    {{/each}}
</div>

<div
    style="width:1000px; height: 25px; border:1.5px solid #000000; display:grid; grid-template-columns: repeat(2, 1fr); grid-template-rows: repeat(1, 1fr); grid-column-gap: 20px; margin:20px 0px 0px 0px">

    {{#if hasPrevPage}}
    <a style="padding: 0% 0% 0% 50%;" href={{{prevLink}}}>Página anterior</a>
    {{/if}}

    {{#if hasNextPage}}
    <a style="padding: 0% 0% 0% 50%;" href={{{nextLink}}}>Página siguiente</a>
    {{/if}}

</div>